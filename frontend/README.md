This is the Next.js frontend for the Signal Clone. See the [repo root README](../README.md) for setup, architecture, database schema, and API overview — the whole app (frontend + backend) runs via `./deploy_local.sh` from the repo root, not from inside this folder.

For local frontend-only development (rare — normally the Docker Compose setup handles this):

```bash
npm install
npm run dev
```
